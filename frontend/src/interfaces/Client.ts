export interface Client {
    id: number;
    username: string; // Added
    first_name: string; // Added
    last_name: string; // Added
    full_name: string;
    email: string;
    telefono: string;
    ciudad: string;
    barrio: string;
    genero: string;
    age: number;
}
